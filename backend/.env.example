# Firebase Admin SDK Configuration
# Download your service account key from Firebase Console:
# Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT_PATH=path/to/serviceAccountKey.json

# Instructions:
# 1. Copy this file to .env
# 2. Download your Firebase service account key JSON file
# 3. Place it in a secure location (e.g., backend/config/serviceAccountKey.json)
# 4. Update the path above to point to your service account key
# 5. Never commit the .env file or service account key to version control
# 6. Add both to .gitignore
